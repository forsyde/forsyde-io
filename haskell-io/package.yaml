name:                forsyde-io-haskell
version:             0.1.0
github:              "githubuser/forsyde-io-haskell"
license:             BSD3
author:              "Author name here"
maintainer:          "example@example.com"
copyright:           "2020 Author name here"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/githubuser/forsyde-io-haskell#readme>

dependencies:
- base >= 4.7 && < 5
- containers >= 0.6.4.1
- hashable >= 1.3.0.0
- regex-base >= 0.94.0.0
- regex-tdfa >= 1.3.1.0

library:
  source-dirs: src

#executables:
  #forsyde-io-haskell-exe:
    #main:                Main.hs
    #source-dirs:         app
    #ghc-options:
    #- -threaded
    #- -rtsopts
    #- -with-rtsopts=-N
    #dependencies:
    #- forsyde-io-haskell

tests:
  forsyde-io-haskell-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - forsyde-io-haskell
